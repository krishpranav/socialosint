#!/usr/bin/env python3
# -*- coding: utf-8 -*-

# imports
import requests
import logging
from .cookie_repository import CookieRepository

logger.getLogger("requests").setLevel(logging.INFO)
logger.getLogger("requests").setLevel(logging.DEBUG)
logger.getLogger("requests").setLevel(logging.WARNING)

logger = logging.getLogger(__name__)

